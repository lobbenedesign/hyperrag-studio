{
  "name": "hyperrag-studio",
  "version": "1.1.0",
  "description": "Local Bun/TypeScript playground: real HNSW vector search + graph RAG toy, plus an honestly-labeled draft/target speculative-decoding benchmark and a structural DSPy-style prompt compiler (both live-scored against local Ollama when available).",
  "main": "server.ts",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts",
    "build": "bun build server.ts --target=bun --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "lightrag",
    "graphrag",
    "speculative-decoding",
    "eagle",
    "medusa",
    "dspy",
    "ai-agent",
    "knowledge-graph"
  ],
  "author": "Giuseppe Lobbene",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
